# Runtime dependencies only. Test tooling lives in requirements-dev.txt so the
# production image does not ship a test framework it will never run.

# ── Web framework ────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ── Numerics & forecasting ───────────────────────────────────
numpy==2.2.1
statsmodels==0.14.4
# Pinned explicitly although it arrives transitively through statsmodels:
# a transitive dependency that the build depends on should be reproducible.
pandas==2.2.3

# ── HTTP ─────────────────────────────────────────────────────
httpx==0.28.1

# ── Observability ────────────────────────────────────────────
structlog==24.4.0
