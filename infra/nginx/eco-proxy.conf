proxy_http_version 1.1;
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host  $host;

# Keepalive to the upstream requires clearing the inherited close header.
proxy_set_header Connection "";

# Next uses websockets for nothing in production, but the upgrade path costs
# nothing to carry and breaks silently if it is missing later.
proxy_set_header Upgrade    $http_upgrade;
